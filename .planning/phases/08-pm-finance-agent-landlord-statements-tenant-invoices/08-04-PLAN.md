---
phase: 08-pm-finance-agent-landlord-statements-tenant-invoices
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-03"]
files_modified:
  - client/src/pages/PendingStatements.tsx
  - client/src/pages/TenantInvoices.tsx
  - client/src/components/CRMLayout.tsx
  - client/src/App.tsx
autonomous: false
requirements: [FIN-02]

must_haves:
  truths:
    - "Staff can see a list of pending (draft) landlord statements awaiting approval"
    - "Staff can approve or reject each statement from the UI"
    - "Staff can send an approved statement (triggers PDF email to landlord)"
    - "Tenant invoices page shows all invoices with status filters"
    - "Finance section visible in CRM sidebar with links to statements and invoices"
  artifacts:
    - path: "client/src/pages/PendingStatements.tsx"
      provides: "Statement approval UI"
    - path: "client/src/pages/TenantInvoices.tsx"
      provides: "Invoice listing page"
    - path: "client/src/components/CRMLayout.tsx"
      provides: "Updated sidebar with Finance section"
    - path: "client/src/App.tsx"
      provides: "Routes for new pages"
  key_links:
    - from: "client/src/pages/PendingStatements.tsx"
      to: "/api/crm/finance/statements/pending"
      via: "useQuery + apiRequest"
      pattern: "finance/statements/pending"
    - from: "client/src/pages/PendingStatements.tsx"
      to: "/api/crm/finance/statements/:id/approve"
      via: "useMutation"
      pattern: "finance/statements.*approve"
    - from: "client/src/App.tsx"
      to: "client/src/pages/PendingStatements.tsx"
      via: "Route component"
      pattern: "PendingStatements"
---

<objective>
Finance UI pages for statement approval and invoice management.

Purpose: Staff need a CRM interface to review, approve, and send landlord statements (the most important output of this phase). Also provides a tenant invoices view for operational visibility. Adds Finance section to CRM sidebar.
Output: PendingStatements.tsx, TenantInvoices.tsx, updated CRMLayout.tsx and App.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pm-finance-agent-landlord-statements-tenant-invoices/08-01-SUMMARY.md
@.planning/phases/08-pm-finance-agent-landlord-statements-tenant-invoices/08-03-SUMMARY.md
@client/src/components/CRMLayout.tsx (sidebar navigation)
@client/src/App.tsx (route definitions -- add BEFORE /crm catch-all)
@client/src/lib/queryClient.ts (apiRequest helper)

<interfaces>
From Plan 03 API endpoints:
```
GET  /api/crm/finance/statements/pending -> { statements: [{ id, landlordName, propertyAddress, period, totalRent, netPayable, attentionNeeded, status, lineItems[] }] }
POST /api/crm/finance/statements/:id/approve -> { success: true }
POST /api/crm/finance/statements/:id/send -> { success: true }
POST /api/crm/finance/statements/:id/reject -> { success: true }
GET  /api/crm/finance/invoices?status=...&tenantId=... -> { invoices: [{ id, invoiceNumber, tenantName, propertyAddress, amount, dueDate, status }] }
POST /api/crm/finance/statements/generate -> { created, attentionNeeded }
POST /api/crm/finance/invoices/generate -> { created, sent }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pending Statements approval page + Tenant Invoices page</name>
  <files>client/src/pages/PendingStatements.tsx, client/src/pages/TenantInvoices.tsx</files>
  <action>
    1. Create client/src/pages/PendingStatements.tsx:
       - useQuery to fetch GET /api/crm/finance/statements/pending via apiRequest
       - Card-based layout showing each pending statement:
         * Property address (bold), landlord name
         * Period (formatted dates)
         * Summary: Rent Collected, Management Fees, Maintenance, VAT, Net Payable (formatted as GBP)
         * Attention needed badge (yellow) if attentionNeeded=true and zero rent
         * Expandable line items table (click to show/hide)
         * Action buttons: "Approve" (green), "Reject" (red outline)
         * After approval, button changes to "Send to Landlord" (purple brand color)
       - useMutation for approve (POST .../approve), send (POST .../send), reject (POST .../reject)
       - After each mutation, invalidate the pending query
       - Toast notifications via useToast on success/error
       - "Generate Statements" button at top (calls POST /api/crm/finance/statements/generate with current month prompt via Dialog -- year/month picker)
       - Empty state: "No pending statements" message
       - Use shadcn/ui components: Card, Button, Badge, Table, Dialog, Select
       - Brand purple (#791E75) for primary actions

    2. Create client/src/pages/TenantInvoices.tsx:
       - useQuery to fetch GET /api/crm/finance/invoices via apiRequest
       - Filterable table with columns: Invoice #, Tenant, Property, Amount, Due Date, Status
       - Filter controls: status dropdown (all, draft, sent, paid, overdue), search by tenant name
       - Status badge colours: draft=gray, sent=blue, paid=green, overdue=red
       - Amount displayed as GBP (divide pence by 100, format with 2 decimals)
       - "Generate Invoices" button (calls POST /api/crm/finance/invoices/generate)
       - Pagination controls
       - Use shadcn/ui components: Table, Badge, Select, Input, Button
  </action>
  <verify>
    <automated>cd "C:/Users/ziaa/Dropbox/ZA/DEV/PROJECTS/JB_CRM" && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>PendingStatements page shows draft statements with approve/reject/send actions; TenantInvoices page shows filterable invoice list; both pages use shadcn/ui with brand styling</done>
</task>

<task type="auto">
  <name>Task 2: CRM sidebar Finance section + App.tsx routing</name>
  <files>client/src/components/CRMLayout.tsx, client/src/App.tsx</files>
  <action>
    1. Read client/src/components/CRMLayout.tsx. Add a "Finance" section to the sidebar navigation (near the existing "Deals" or "Accounting" section). Use an appropriate icon (e.g., PoundSterling or Receipt from lucide-react). Items:
       - "Pending Statements" -> /crm/finance/statements
       - "Tenant Invoices" -> /crm/finance/invoices

    2. Read client/src/App.tsx. Add route imports and Route entries:
       - Import PendingStatements and TenantInvoices (lazy or direct)
       - Add routes BEFORE the /crm catch-all (this is CRITICAL -- wouter matches first route):
         ```
         <Route path="/crm/finance/statements"><PendingStatements /></Route>
         <Route path="/crm/finance/invoices"><TenantInvoices /></Route>
         ```
       - Wrap in ProtectedRoute for auth protection
  </action>
  <verify>
    <automated>cd "C:/Users/ziaa/Dropbox/ZA/DEV/PROJECTS/JB_CRM" && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>Finance section in sidebar with 2 links; routes render correctly; pages accessible at /crm/finance/statements and /crm/finance/invoices</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify finance UI end-to-end</name>
  <files>n/a</files>
  <action>
    Human verifies the complete finance UI works as expected.
  </action>
  <verify>
    <automated>echo "Manual verification checkpoint"</automated>
  </verify>
  <done>User confirms finance pages render, sidebar links work, and statement approval workflow is functional</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- /crm/finance/statements renders PendingStatements
- /crm/finance/invoices renders TenantInvoices
- Finance section visible in CRM sidebar
- Statement approve/send workflow functional
</verification>

<success_criteria>
Staff can navigate to Finance section in CRM, view pending statements, approve/reject/send them, and view tenant invoices with filters. Complete end-to-end workflow from auto-generation to landlord email delivery.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pm-finance-agent-landlord-statements-tenant-invoices/08-04-SUMMARY.md`
</output>
