---
phase: 01-foundation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [client/src/App.tsx]
autonomous: true
requirements: [KB-05]
gap_closure: true

must_haves:
  truths:
    - "Navigating to /crm/properties/:id/knowledge-base renders PropertyKnowledgeBase, not MyDesk"
  artifacts:
    - path: "client/src/App.tsx"
      provides: "Correct route ordering for knowledge-base"
      contains: "knowledge-base.*before.*/crm.*catch-all"
  key_links:
    - from: "client/src/App.tsx"
      to: "client/src/pages/PropertyKnowledgeBase.tsx"
      via: "Route path match in Switch"
      pattern: "path=\"/crm/properties/:id/knowledge-base\""
---

<objective>
Fix the wouter route ordering bug that prevents the Property Knowledge Base page from rendering.

Purpose: The knowledge-base route at line 297 is placed after the /crm catch-all at line 293. Wouter's Switch matches top-to-bottom with loose prefix matching, so /crm intercepts /crm/properties/:id/knowledge-base and renders MyDesk instead.
Output: PropertyKnowledgeBase page reachable at /crm/properties/:id/knowledge-base
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@client/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move knowledge-base route before /crm catch-all</name>
  <files>client/src/App.tsx</files>
  <action>
In client/src/App.tsx, move the knowledge-base Route (currently line 297):
```
<Route path="/crm/properties/:id/knowledge-base">{(params: any) => <CRMLayout><PropertyKnowledgeBase {...params} /></CRMLayout>}</Route>
```
to BEFORE the /crm catch-all route (currently line 293):
```
<Route path="/crm"><CRMLayout><MyDesk /></CRMLayout></Route>
```

Also move the other specific /crm/* routes that are currently AFTER the catch-all (lines 294-300) to BEFORE it. The /crm catch-all must be the LAST /crm route in the Switch block.

Specifically, move these routes to before the catch-all:
- /crm/dashboard
- /crm/properties/create
- /crm/properties/import
- /crm/properties/:id/knowledge-base
- /crm/properties/:id/edit
- /crm/properties/:id
- /crm/properties

Then place `<Route path="/crm">` after all of those.

This follows the documented pattern in CLAUDE.md memory: "NEW routes MUST be placed BEFORE Route path=/crm."
  </action>
  <verify>
    <automated>grep -n "knowledge-base\|path=\"/crm\">" client/src/App.tsx | head -10</automated>
  </verify>
  <done>The knowledge-base route line number is lower than the /crm catch-all route line number in App.tsx. All specific /crm/* routes appear before the catch-all.</done>
</task>

</tasks>

<verification>
1. grep confirms knowledge-base route appears before /crm catch-all in line number order
2. TypeScript compiles without errors: npx tsc --noEmit (or npm run check)
</verification>

<success_criteria>
- /crm/properties/:id/knowledge-base route is placed before /crm catch-all in App.tsx
- No TypeScript compilation errors
- KB-05 gap from VERIFICATION.md is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
