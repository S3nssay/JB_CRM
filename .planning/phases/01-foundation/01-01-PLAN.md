---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - vitest.config.ts
  - package.json
  - tests/tools/queryKnowledgeBase.test.ts
  - tests/schema/propertySystemsInventory.test.ts
  - tests/tools/registry.test.ts
  - tests/channels/conversationStore.test.ts
  - tests/channels/contactIdentity.test.ts
  - tests/channels/gateway.test.ts
  - tests/audit/auditLogger.test.ts
  - tests/middleware/aiIdentification.test.ts
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03
  - AGENT-04
  - AGENT-05
  - CHAN-01
  - CHAN-02

must_haves:
  truths:
    - "property_systems_inventory table exists in the database with columns for systemType, make, model, warrantyExpiryDate, lastServiceDate"
    - "agent_audit_log table exists in the database with columns for agentType, action, toolName, reasoning, channel"
    - "contact_identity table exists in the database with identifierType, identifierValue, contactId, contactType"
    - "conversations table has new nullable columns: source, agent_type, contact_type, last_channel, resolved_contact_id"
    - "messages table has new nullable columns: agent_type, tool_calls, is_ai_generated"
    - "Vitest runs and all test stub files exist"
  artifacts:
    - path: "shared/schema.ts"
      provides: "New table definitions: propertySystemsInventory, agentAuditLog, contactIdentities + extended conversations/messages"
      contains: "property_systems_inventory"
    - path: "vitest.config.ts"
      provides: "Vitest configuration"
    - path: "tests/tools/registry.test.ts"
      provides: "Test stubs for Tool Registry"
    - path: "tests/channels/gateway.test.ts"
      provides: "Test stubs for Channel Gateway"
  key_links:
    - from: "shared/schema.ts"
      to: "database"
      via: "npm run db:push"
      pattern: "pgTable.*property_systems_inventory"
---

<objective>
Create all new database schema tables (property systems inventory, agent audit log, contact identities), extend existing conversations/messages tables with AI agent fields, install Vitest, and create all Wave 0 test stubs for the entire phase.

Purpose: This is the foundation for all other Phase 1 plans. Every subsequent plan depends on these tables existing and Vitest being available.
Output: Updated shared/schema.ts with 3 new tables + 2 extended tables, vitest.config.ts, and 9 test stub files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@shared/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new schema tables and extend existing tables</name>
  <files>shared/schema.ts</files>
  <action>
Add three new table definitions to shared/schema.ts and extend two existing tables. CRITICAL: Before writing ANY code, grep shared/schema.ts for existing column names on conversations and messages tables. Copy-paste exact names.

**New Table 1: propertySystemsInventory** (pgTable name: "property_systems_inventory")
Add after the propertyCertifications table (~line 1371). Fields:
- id: serial primary key
- propertyId: integer("property_id").notNull()
- systemType: text("system_type").notNull() -- 'heating', 'hot_water', 'electrical', 'plumbing', 'ventilation', 'fire_safety', 'security', 'other'
- make: text("make") -- e.g. 'Vaillant', 'Worcester Bosch'
- model: text("model") -- e.g. 'EcoTEC Plus 832'
- serialNumber: text("serial_number")
- installedDate: timestamp("installed_date")
- installedBy: text("installed_by")
- contractorId: integer("contractor_id") -- FK to contractors
- warrantyExpiryDate: timestamp("warranty_expiry_date")
- lastServiceDate: timestamp("last_service_date")
- nextServiceDue: timestamp("next_service_due")
- serviceIntervalMonths: integer("service_interval_months")
- location: text("location") -- e.g. 'Kitchen cupboard', 'Loft'
- notes: text("notes")
- specifications: json("specifications")
- lastVerifiedAt: timestamp("last_verified_at")
- lastVerifiedBy: integer("last_verified_by")
- createdAt: timestamp("created_at").notNull().defaultNow()
- updatedAt: timestamp("updated_at").notNull().defaultNow()

Also export: insertPropertySystemsInventorySchema (createInsertSchema), PropertySystemsInventory type ($inferSelect), InsertPropertySystemsInventory type ($inferInsert).

**New Table 2: agentAuditLog** (pgTable name: "agent_audit_log")
Add near the end of the schema file. Fields:
- id: serial primary key
- conversationId: integer("conversation_id") -- nullable FK to conversations
- messageId: integer("message_id") -- nullable
- agentType: text("agent_type").notNull() -- 'supervisor', 'sales', 'rental', 'maintenance', 'office_admin', etc.
- action: text("action").notNull() -- 'classify', 'route', 'tool_call', 'respond', 'escalate', 'identify_ai'
- toolName: text("tool_name") -- nullable, only for tool_call actions
- toolInput: json("tool_input") -- nullable
- toolOutput: json("tool_output") -- nullable
- reasoning: text("reasoning")
- confidence: numeric("confidence", { precision: 3, scale: 2 })
- durationMs: integer("duration_ms")
- channel: text("channel") -- 'sms', 'whatsapp', 'phone', 'email', 'web_chat'
- error: text("error") -- nullable
- createdAt: timestamp("created_at").notNull().defaultNow()

Export: insertAgentAuditLogSchema, AgentAuditLog type, InsertAgentAuditLog type.

**New Table 3: contactIdentities** (pgTable name: "contact_identity")
Add near conversations table. Fields:
- id: serial primary key
- contactId: integer("contact_id").notNull()
- contactType: text("contact_type").notNull() -- 'lead', 'tenant', 'landlord', 'user'
- identifierType: text("identifier_type").notNull() -- 'phone', 'email', 'whatsapp'
- identifierValue: text("identifier_value").notNull() -- E.164 phone or email
- isPrimary: boolean("is_primary").default(false)
- verified: boolean("verified").default(false)
- createdAt: timestamp("created_at").notNull().defaultNow()
Add unique constraint on (identifierType, identifierValue) using the table callback pattern.

Export: insertContactIdentitySchema, ContactIdentity type, InsertContactIdentity type.

**Extend existing conversations table** (~line 3474):
Add these NULLABLE columns after the existing fields:
- source: text("source").default("human") -- 'human' or 'ai_agent'
- agentType: text("agent_type") -- which agent is handling
- contactType: text("contact_type") -- 'lead', 'tenant', 'landlord', 'unknown'
- lastChannel: text("last_channel") -- last channel used
- resolvedContactId: integer("resolved_contact_id") -- cross-ref to contact_identity

**Extend existing messages table** (~line 3507):
Add these NULLABLE columns after the existing fields:
- agentType: text("agent_type") -- which agent handled
- toolCalls: json("tool_calls") -- tools invoked during processing
- isAiGenerated: boolean("is_ai_generated").default(false)

IMPORTANT: All new columns on existing tables MUST be nullable (no .notNull()) to avoid breaking existing queries and data.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>shared/schema.ts has 3 new tables (propertySystemsInventory, agentAuditLog, contactIdentities) and 2 extended tables (conversations, messages) with all types exported. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Install Vitest, create config, push schema, and create all test stubs</name>
  <files>package.json, vitest.config.ts, tests/tools/queryKnowledgeBase.test.ts, tests/schema/propertySystemsInventory.test.ts, tests/tools/registry.test.ts, tests/channels/conversationStore.test.ts, tests/channels/contactIdentity.test.ts, tests/channels/gateway.test.ts, tests/audit/auditLogger.test.ts, tests/middleware/aiIdentification.test.ts</files>
  <action>
1. Install Vitest: `npm install -D vitest`

2. Create vitest.config.ts at project root:
```typescript
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['tests/**/*.test.ts'],
    exclude: ['node_modules'],
  },
  resolve: {
    alias: {
      '@shared': path.resolve(__dirname, './shared'),
      '@': path.resolve(__dirname, './client/src'),
    },
  },
});
```

3. Push schema changes to database: Run `npm run db:push` non-interactively OR use direct SQL via `node -e "..."` to create the 3 new tables and add columns to conversations/messages. Verify tables exist by querying information_schema.

4. Create test stub files (each with describe blocks and placeholder test cases that will be filled in by later plans):

**tests/tools/queryKnowledgeBase.test.ts** - stubs for:
- "certifications" - returns certification records with expiry dates (KB-01)
- "maintenance" - returns maintenance history linked to contractors (KB-03)
- "performance" - query completes under 100ms (KB-04)

**tests/schema/propertySystemsInventory.test.ts** - stubs for:
- "systems inventory CRUD" - can insert and query systems inventory records (KB-02)

**tests/tools/registry.test.ts** - stubs for:
- "registers a tool" - tool is registered by name
- "invokes a tool with valid input" - tool executes and returns typed output
- "rejects unauthorized agent type" - permission check works
- "validates input with Zod" - invalid input throws ZodError (AGENT-02)

**tests/channels/conversationStore.test.ts** - stubs for:
- "creates conversation with AI agent fields" - source, agentType persisted (AGENT-04)
- "continues existing conversation" - messages append to same thread

**tests/channels/contactIdentity.test.ts** - stubs for:
- "resolves known phone to existing contact" - returns correct contactId (CHAN-02)
- "creates new identity for unknown sender" - new row created
- "normalizes phone to E.164" - +44 and 07 formats resolve to same identity

**tests/channels/gateway.test.ts** - stubs for:
- "threading" - "threads SMS into existing conversation" (CHAN-01)
- "threading" - "threads WhatsApp into existing conversation" (CHAN-01)
- "creates new conversation for unknown sender" (CHAN-01)

**tests/audit/auditLogger.test.ts** - stubs for:
- "logs tool call to agent_audit_log" - row created with correct fields (AGENT-05)
- "logs classification decision" - non-tool action logged (AGENT-05)

**tests/middleware/aiIdentification.test.ts** - stubs for:
- "prepends AI identification to first message" (AGENT-06)
- "does not repeat identification on subsequent messages" (AGENT-06)

Each test stub should use `describe` and `it` with `it.todo('...')` for unimplemented tests and a single passing sanity test:
```typescript
import { describe, it, expect } from 'vitest';

describe('FeatureName', () => {
  it('sanity check', () => {
    expect(true).toBe(true);
  });
  it.todo('actual test description');
});
```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest installed and configured. All 9 test stub files exist with .todo placeholders. `npx vitest run` executes without errors (sanity tests pass, todos are skipped). New tables exist in the database (verified via information_schema query).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (schema types valid)
- `npx vitest run` shows all sanity tests passing, todos listed
- Database has new tables: property_systems_inventory, agent_audit_log, contact_identity
- Database has extended columns on conversation and message tables
</verification>

<success_criteria>
- 3 new Drizzle tables defined and pushed to database
- 2 existing tables extended with nullable AI agent columns
- Vitest installed and configured with path aliases
- 9 test stub files created covering all Phase 1 requirements
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
