---
phase: 10-business-accounts-agent-company-wide-financials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/accountingQueries.ts
  - server/agents/sdk/businessAccountsAgent.ts
  - server/agents/sdk/supervisorAgent.ts
  - server/__tests__/businessAccountsAgent.test.ts
autonomous: true
requirements:
  - BIZ-01
  - BIZ-02
  - BIZ-03
  - BIZ-04
  - BIZ-05
  - BIZ-07
  - BIZ-08

must_haves:
  truths:
    - "Staff can ask Riley about P&L for a date range and get correct revenue/expense/net profit"
    - "Staff can ask Riley for a balance sheet and get assets/liabilities/equity that balance"
    - "Staff can ask Riley about VAT return status and get correct HMRC box figures"
    - "Staff can ask Riley to close a financial period and it updates status"
    - "Staff can ask Riley about aged debtors/creditors and get correct ageing buckets"
    - "Staff can ask Riley about cash position and get correct bank account balances"
    - "Supervisor routes internal finance queries to Riley, not to Taylor or other agents"
  artifacts:
    - path: "server/accountingQueries.ts"
      provides: "Shared query functions extracted from accountingRoutes.ts patterns"
      exports: ["getTrialBalance", "getProfitAndLoss", "getBalanceSheet", "getCashPosition", "getAgedDebtors", "getAgedCreditors", "getFinancialYearDates"]
    - path: "server/agents/sdk/businessAccountsAgent.ts"
      provides: "Riley agent definition with all financial query tools"
      exports: ["businessAccountsAgent"]
    - path: "server/agents/sdk/supervisorAgent.ts"
      provides: "Updated Supervisor with Riley handoff"
      contains: "transfer_to_business_accounts"
    - path: "server/__tests__/businessAccountsAgent.test.ts"
      provides: "Unit tests for BIZ-01 through BIZ-08"
      min_lines: 80
  key_links:
    - from: "server/agents/sdk/businessAccountsAgent.ts"
      to: "server/accountingQueries.ts"
      via: "lazy import in tool execute functions"
      pattern: "import.*accountingQueries"
    - from: "server/agents/sdk/supervisorAgent.ts"
      to: "server/agents/sdk/businessAccountsAgent.ts"
      via: "handoff(businessAccountsAgent)"
      pattern: "handoff\\(businessAccountsAgent"
---

<objective>
Create Riley, the Business Accounts AI agent, with all financial query tools wrapping the existing accounting system, plus register Riley in the Supervisor for staff finance queries.

Purpose: Give staff conversational access to company-wide financials (P&L, balance sheet, VAT, cash position, aged debtors/creditors, financial period management) without navigating 20+ accounting UI pages.

Output: Riley agent definition, shared accounting query module, Supervisor handoff, unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-business-accounts-agent-company-wide-financials/10-RESEARCH.md

@server/agents/sdk/supervisorAgent.ts
@server/agents/sdk/context.ts
@server/agents/sdk/arrearsAgent.ts
@server/agents/sdk/tools.ts
@server/accountingRoutes.ts
@server/accountingIntegration.ts
@shared/schema.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From server/agents/sdk/context.ts:
```typescript
export interface AgentContext {
  conversationId: number;
  contactId: number;
  channel: CommunicationChannel;
  isFirstMessage: boolean;
  agentType: AgentType;
}
```

From server/agents/sdk/supervisorAgent.ts (handoff pattern):
```typescript
import { Agent, handoff } from '@openai/agents';
// handoffs array:
handoff(salesAgent, {
  toolNameOverride: 'transfer_to_sales',
  toolDescription: 'Transfer to Sales for property purchase enquiries...',
}),
```

From server/agents/sdk/tools.ts:
```typescript
export const escalateToHumanTool = tool({ ... });
// All tools use: import { tool } from '@openai/agents'; import { z as z4 } from 'zod4';
```

Existing accounting route patterns (server/accountingRoutes.ts):
- GET /accounting/trial-balance (line 630) -- base query for P&L and balance sheet
- GET /accounting/financial-periods (line 308) -- list periods with open/closed/locked status
- POST /accounting/financial-periods/:id/close (line 361) -- close a period
- POST /accounting/vat-returns/:id/calculate (line 1550) -- VAT calculation
- GET /accounting/reports/aged-debtors (line 1372) -- 30/60/90/120+ day buckets
- GET /accounting/reports/aged-creditors (line 1436) -- 30/60/90/120+ day buckets
- GET /accounting/dashboard (line 1670) -- cash balance, receivables, payables
- GET /accounting/business-settings (line 34) -- financial year start month

From server/accountingIntegration.ts:
```typescript
export async function accountingRecordRentPayment(...)
export async function accountingRecordManagementFee(...)
export async function accountingRecordLandlordPayment(...)
export async function accountingRecordExpense(...)
export async function accountingRecordBusinessInvoiceSent(...)
export async function accountingRecordBusinessInvoicePayment(...)
export async function accountingRecordPurchaseInvoice(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared accounting queries module + Riley agent with financial tools</name>
  <files>server/accountingQueries.ts, server/agents/sdk/businessAccountsAgent.ts, server/__tests__/businessAccountsAgent.test.ts</files>
  <behavior>
    - Test: getTrialBalance returns accounts with correct balance calculation (debit normal_balance: opening + debits - credits; credit normal_balance: opening + credits - debits)
    - Test: getProfitAndLoss filters to revenue + expense accounts and computes totalRevenue, totalExpenses, netProfit
    - Test: getBalanceSheet filters to asset + liability + equity accounts and verifies assets = liabilities + equity
    - Test: getCashPosition sums bank account balances (account codes starting with 1010, 1020)
    - Test: getAgedDebtors returns correct 30/60/90/120+ day ageing buckets
    - Test: getAgedCreditors returns correct ageing buckets
    - Test: getFinancialYearDates respects businessSettings.financialYearStart (defaults to April)
    - Test: businessAccountsAgent has name 'Riley from Business Accounts' and model 'gpt-4o'
    - Test: businessAccountsAgent has tools array with expected tool names
    - Test: Riley instructions mention amounts are in pence and must be converted to GBP
    - Test: Riley instructions explicitly exclude tenant/landlord queries (Taylor's domain)
  </behavior>
  <action>
    1. Create server/accountingQueries.ts -- extract shared query functions from patterns in accountingRoutes.ts:
       - getTrialBalance(asAt?: string): queries chart_of_accounts LEFT JOIN journal_entry_lines/journal_entries (posted only), computes balance per account using normal_balance direction, returns {accounts, totalDebitBalances, totalCreditBalances}
       - getProfitAndLoss(startDate: string, endDate: string): filters trial balance to revenue + expense account_types, computes totalRevenue (credits - debits for revenue), totalExpenses (debits - credits for expense), netProfit
       - getBalanceSheet(asAt: string): filters trial balance to asset + liability + equity account_types, returns categorized with totals
       - getCashPosition(): queries balances for bank accounts (codes 1010 Business Current, 1020 Client Money), returns {businessAccount, clientAccount, total}
       - getAgedDebtors(asAt?: string): replicates aged-debtors report query from accountingRoutes.ts line 1372 -- queries business_invoices with status='sent', groups by customer into 30/60/90/120+ day buckets
       - getAgedCreditors(asAt?: string): replicates aged-creditors report query from line 1436 -- queries purchase_invoices with status IN ('approved','overdue'), groups by supplier into ageing buckets
       - getFinancialYearDates(offset?: number): reads business_settings.financial_year_start (defaults 4 = April), computes start/end dates for current + offset FY
       All functions use lazy import for pool: `const { pool } = await import('./db');`
       All amounts in pence (integer). Export TrialBalanceRow interface.

    2. Create server/agents/sdk/businessAccountsAgent.ts following arrearsAgent.ts pattern:
       - Import { Agent, tool } from '@openai/agents', { z as z4 } from 'zod4'
       - BUSINESS_ACCOUNTS_INSTRUCTIONS persona for Riley:
         * Staff-only agent for company-wide financials
         * Handles P&L, balance sheet, VAT, cash position, aged debtors/creditors, financial periods
         * Does NOT handle tenant rent invoices or landlord statements (Taylor's domain)
         * Does NOT handle rent arrears (Sarah's domain)
         * All tool results are in pence internally; convert to GBP for display using (amount/100).toFixed(2)
         * Respect financial year start from business settings (not calendar year)
         * British English, no emoji, professional tone
       - Define tools (each using lazy imports for accountingQueries):
         * query_profit_and_loss: params {startDate, endDate}, calls getProfitAndLoss
         * query_balance_sheet: params {asAt}, calls getBalanceSheet
         * query_trial_balance: params {asAt?}, calls getTrialBalance
         * query_vat_return: params {vatReturnId}, queries vat_returns + vat_return_transactions
         * calculate_vat_return: params {vatReturnId}, calls existing calculate logic from accountingRoutes line 1550
         * query_cash_position: no params, calls getCashPosition
         * query_aged_debtors: params {asAt?}, calls getAgedDebtors
         * query_aged_creditors: params {asAt?}, calls getAgedCreditors
         * query_financial_periods: no params, queries financial_periods table ordered by start_date desc
         * close_financial_period: params {periodId}, validates period is 'open', updates to 'closed'
         * query_financial_year_dates: params {offset?}, calls getFinancialYearDates
         * escalate_to_human: reuse escalateToHumanTool from tools.ts
       - Export businessAccountsAgent as Agent<AgentContext>

    3. Create server/__tests__/businessAccountsAgent.test.ts with:
       - Static analysis tests (no DB needed): verify agent name, model, tools array, instruction content
       - Mock pool.query tests for each accountingQueries function
       - Use vi.mock for pool, test query result transformation logic

    CRITICAL: Use zod4 alias (not zod) for all tool parameter schemas per project convention.
    CRITICAL: Lazy import pattern for pool in all tool execute functions (avoid DB connection at module load).
    CRITICAL: All amounts stored/returned in pence. Agent instructions must state this clearly.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/businessAccountsAgent.test.ts</automated>
  </verify>
  <done>
    - accountingQueries.ts exports 7 shared query functions
    - businessAccountsAgent.ts exports Riley agent with 12 tools
    - All tests pass covering agent definition, tool names, instruction content, and query logic
  </done>
</task>

<task type="auto">
  <name>Task 2: Register Riley in Supervisor with finance routing</name>
  <files>server/agents/sdk/supervisorAgent.ts</files>
  <action>
    1. Add import for businessAccountsAgent at top of supervisorAgent.ts:
       `import { businessAccountsAgent } from './businessAccountsAgent';`

    2. Add Riley handoff to the Supervisor's handoffs array (after the existing pmAgent handoff):
       ```
       handoff(businessAccountsAgent, {
         toolNameOverride: 'transfer_to_business_accounts',
         toolDescription: 'Transfer to Business Accounts (Riley) for company-wide financial queries from staff: profit and loss, balance sheet, VAT returns, cash position, aged debtors/creditors, financial periods, journal entries. NOT for tenant rent invoices or landlord statements (use Taylor for those).',
       }),
       ```

    3. Update SUPERVISOR_INSTRUCTIONS to add Riley:
       - Add line: "- Riley from Business Accounts handles company-wide financial queries: P&L reports, balance sheets, VAT returns, cash position, aged debtors/creditors, financial period management."
       - Add routing rule: "When a staff member asks about business-level financials (P&L, balance sheet, VAT, cash flow), route to Riley. When a tenant or landlord asks about their own invoices or statements, route to Taylor (if available) or escalate."

    4. Verify existing agent imports and handoffs are preserved (Sales, Lettings, Admin, PM).
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/businessAccountsAgent.test.ts -t "supervisor"</automated>
  </verify>
  <done>
    - Supervisor has 5 handoffs: Sales, Lettings, Admin, PM, Business Accounts
    - transfer_to_business_accounts tool description clearly distinguishes Riley from Taylor
    - Supervisor instructions include Riley routing guidance
  </done>
</task>

</tasks>

<verification>
- `npx vitest run server/__tests__/businessAccountsAgent.test.ts` -- all tests green
- `npx vitest run` -- full suite green (no regressions)
- Static verification: grep supervisorAgent.ts for 'businessAccountsAgent' import and handoff
- Static verification: grep businessAccountsAgent.ts for all 12 tool names
</verification>

<success_criteria>
- Riley agent defined with persona, 12 financial tools, and correct model (gpt-4o)
- Shared accountingQueries module extracts reusable query functions from existing patterns
- Supervisor routes staff finance queries to Riley via transfer_to_business_accounts handoff
- All amounts handled in pence internally with GBP conversion in agent instructions
- Financial year boundary respected (configurable start month, not calendar year)
- Tests cover agent definition, tool names, query logic, and Supervisor registration
</success_criteria>

<output>
After completion, create `.planning/phases/10-business-accounts-agent-company-wide-financials/10-01-SUMMARY.md`
</output>
