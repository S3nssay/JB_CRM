---
phase: 07-agent-corrections-cost-ledger
plan: 04
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - client/src/pages/OffersManagement.tsx
  - client/src/components/OffersSection.tsx
  - client/src/components/CostLedger.tsx
  - client/src/pages/ManagedPropertyCard.tsx
  - client/src/pages/LandlordDirectory.tsx
  - client/src/components/CRMLayout.tsx
  - client/src/App.tsx
autonomous: false
requirements: [OFFER-UI, COST-UI]

must_haves:
  truths:
    - "Staff can view all active offers across properties in a central dashboard"
    - "Staff can accept, reject, or counter an offer from the offers dashboard or property page"
    - "Property detail page shows offers section with all offers for that property"
    - "Property detail page shows cost ledger with maintenance and compliance breakdown"
    - "Landlord detail page shows cost ledger with per-property cost breakdown"
    - "Offers dashboard is accessible from CRM sidebar navigation"
  artifacts:
    - path: "client/src/pages/OffersManagement.tsx"
      provides: "Central offers dashboard with filters and status actions"
      min_lines: 100
    - path: "client/src/components/OffersSection.tsx"
      provides: "Reusable offers section for property detail page"
      min_lines: 50
    - path: "client/src/components/CostLedger.tsx"
      provides: "Reusable cost ledger component for property and landlord pages"
      min_lines: 80
    - path: "client/src/App.tsx"
      provides: "Route for /crm/offers BEFORE /crm catch-all"
      contains: "OffersManagement"
  key_links:
    - from: "client/src/pages/OffersManagement.tsx"
      to: "/api/crm/offers"
      via: "useQuery + useMutation"
      pattern: "api/crm/offers"
    - from: "client/src/components/CostLedger.tsx"
      to: "/api/crm/properties/:id/costs"
      via: "useQuery"
      pattern: "api/crm/.*costs"
    - from: "client/src/App.tsx"
      to: "client/src/pages/OffersManagement.tsx"
      via: "Route path before /crm catch-all"
      pattern: "crm/offers"
---

<objective>
Build the offers management dashboard, offers section for property pages, and cost ledger component for property and landlord pages. Wire all routes and navigation.

Purpose: Staff can manage offers and view costs directly in the CRM without switching to external tools.
Output: OffersManagement page, OffersSection component, CostLedger component, route and nav wiring
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-agent-corrections-cost-ledger/07-CONTEXT.md
@.planning/phases/07-agent-corrections-cost-ledger/07-02-SUMMARY.md
@.planning/phases/07-agent-corrections-cost-ledger/07-03-SUMMARY.md

@client/src/App.tsx (route definitions -- add BEFORE /crm catch-all)
@client/src/components/CRMLayout.tsx (sidebar navigation)
@client/src/pages/ManagedPropertyCard.tsx (property detail -- add Offers tab + Cost Ledger tab)
@client/src/pages/LandlordDirectory.tsx (landlord pages -- add Cost Ledger section)

<interfaces>
<!-- Offer API endpoints (from Plan 02) -->
GET /api/crm/offers?propertyId=&status=&dateFrom=&dateTo=
GET /api/crm/offers/:id
POST /api/crm/offers
PATCH /api/crm/offers/:id/status { status, rejectionReason?, counterOfferAmount?, finalAgreedAmount? }
GET /api/crm/properties/:id/offers

<!-- Cost Ledger API endpoints (from Plan 03) -->
GET /api/crm/properties/:id/costs -> { maintenance: { items, total }, compliance: { items, total }, grandTotal }
GET /api/crm/landlords/:id/costs -> { properties: [{ propertyId, address, maintenanceTotal, complianceTotal }], grandTotal }
GET /api/crm/properties/:id/cost-threshold -> threshold object or null
PUT /api/crm/properties/:id/cost-threshold { annualLimit, notificationEmail }
DELETE /api/crm/properties/:id/cost-threshold

<!-- propertyOffers fields -->
id, propertyId, offerAmount (pence), buyerName, buyerEmail, buyerPhone, buyerPosition,
isInChain, chainDetails, conditions[], proposedCompletionDate, status (pending/under_review/accepted/rejected/withdrawn),
counterOfferAmount, counterOfferDate, finalAgreedAmount, decisionDate, decisionBy, rejectionReason,
employmentStatus, rentalReferences, moveInTimeline, offerSource, createdAt, updatedAt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offers dashboard page and offers section component</name>
  <files>client/src/pages/OffersManagement.tsx, client/src/components/OffersSection.tsx</files>
  <action>
  1. Create `client/src/pages/OffersManagement.tsx` -- Central offers dashboard:
     - Use TanStack React Query to fetch GET /api/crm/offers
     - Filter controls: property search/select, status dropdown (all/pending/under_review/accepted/rejected/withdrawn), date range
     - Table layout using shadcn Table component showing: property address, offer amount (formatted GBP from pence), buyer name, buyer position badge, chain status, date, status badge
     - Row click expands to show full details: email, phone, conditions, chain details, proposed completion
     - Action buttons per row:
       - "Accept" (green) -- calls PATCH /offers/:id/status with status='accepted'
       - "Reject" (red) -- opens Dialog for rejection reason, then PATCH with status='rejected'
       - "Counter" -- opens Dialog for counter amount input, then PATCH with counterOfferAmount and status='under_review'
     - Use useMutation with queryClient.invalidateQueries on success
     - Format amounts: divide pence by 100, display as GBP with Intl.NumberFormat('en-GB', {style:'currency',currency:'GBP'})
     - Status badges: pending=yellow, under_review=blue, accepted=green, rejected=red, withdrawn=gray
     - Buyer position badges: cash=green, mortgage_approved=blue, mortgage_required=amber, chain=orange
     - Default export

  2. Create `client/src/components/OffersSection.tsx` -- Reusable for property detail page:
     - Props: `propertyId: number`
     - Fetches GET /api/crm/properties/:id/offers
     - Same table layout as dashboard but without property column (already on property page)
     - Same action buttons (accept/reject/counter)
     - Shows empty state: "No offers received yet"
     - Includes lettings-specific fields when present: employment status, references, move-in timeline
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>OffersManagement page renders filterable offers table with accept/reject/counter actions. OffersSection component renders property-specific offers with same actions.</done>
</task>

<task type="auto">
  <name>Task 2: Cost ledger component, page wiring, and navigation</name>
  <files>client/src/components/CostLedger.tsx, client/src/pages/ManagedPropertyCard.tsx, client/src/pages/LandlordDirectory.tsx, client/src/components/CRMLayout.tsx, client/src/App.tsx</files>
  <action>
  1. Create `client/src/components/CostLedger.tsx` -- Reusable cost ledger component:
     - Props: `mode: 'property' | 'landlord'`, `entityId: number`
     - Property mode: fetches GET /api/crm/properties/:id/costs
       - Shows running total card at top (grand total)
       - Category breakdown: two cards showing maintenance total and compliance total
       - Two tables: maintenance expenses (work order number, scope, issue type, invoice amount, date) and compliance expenses (certification type, issued by, inspection date, cost)
       - Threshold configuration section: shows current threshold if set, "Set Threshold" button opens Dialog with annual limit input (in GBP, converts to pence) and notification email input. Uses PUT endpoint. Delete button to remove threshold.
     - Landlord mode: fetches GET /api/crm/landlords/:id/costs
       - Shows grand total card
       - Per-property table: property address, maintenance total, compliance total, combined total
       - No threshold config in landlord view (thresholds are per-property)
     - Format all amounts from pence to GBP with Intl.NumberFormat
     - Empty state: "No costs recorded yet"

  2. In `client/src/pages/ManagedPropertyCard.tsx`:
     - Read the file first to understand current tab structure
     - Add "Offers" tab using OffersSection component (pass propertyId)
     - Add "Costs" tab using CostLedger component (mode='property', entityId=propertyId)
     - Import OffersSection and CostLedger

  3. In `client/src/pages/LandlordDirectory.tsx`:
     - Read the file first to understand current structure
     - If landlord detail view exists, add "Costs" section using CostLedger component (mode='landlord', entityId=landlordId)
     - If it's a list page with expandable rows, add CostLedger in the expanded detail section
     - Import CostLedger

  4. In `client/src/components/CRMLayout.tsx`:
     - Read the file first to find the navigation section
     - Add "Offers" link to the sidebar navigation, icon: Handshake or DollarSign from lucide-react
     - Place in appropriate section (near deals or as standalone)

  5. In `client/src/App.tsx`:
     - CRITICAL: Read the file first. Add the OffersManagement route BEFORE the /crm catch-all route (see MEMORY.md routing rules)
     - Add: `<Route path="/crm/offers"><OffersManagement /></Route>`
     - Add lazy import for OffersManagement at the top
     - The /crm catch-all is the route that matches ALL /crm/* paths -- new routes MUST go before it
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>CostLedger component renders in both property and landlord modes. ManagedPropertyCard has Offers and Costs tabs. LandlordDirectory has Costs section. CRMLayout has Offers nav link. App.tsx has /crm/offers route before /crm catch-all.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of offers and cost ledger UI</name>
  <files>client/src/pages/OffersManagement.tsx, client/src/components/CostLedger.tsx</files>
  <action>
  Human verifies the complete offers management UI and cost ledger UI across property and landlord pages.

  What was built:
  - Central offers dashboard at /crm/offers with filters and accept/reject/counter actions
  - Offers section on property detail page
  - Cost ledger component on property detail page (maintenance + compliance breakdown)
  - Cost ledger component on landlord page (per-property cost breakdown)
  - Sidebar navigation link to offers dashboard

  How to verify:
  1. Navigate to /crm/offers -- verify the offers dashboard loads with filter controls and table
  2. Check CRM sidebar -- verify "Offers" link appears and navigates to /crm/offers
  3. Navigate to a managed property detail page -- verify "Offers" tab and "Costs" tab appear
  4. Click "Costs" tab on property -- verify maintenance and compliance cost sections render
  5. Navigate to landlord directory -- verify cost ledger section shows per-property breakdown
  6. If test data exists: verify amounts display in GBP format (not raw pence)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>User confirms all UI elements render correctly and navigation works. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /crm/offers route accessible (not caught by /crm catch-all)
- OffersManagement page renders with filters and action buttons
- CostLedger component works in both property and landlord modes
- Sidebar navigation includes Offers link
</verification>

<success_criteria>
- Staff can view, filter, accept, reject, and counter offers from /crm/offers
- Property detail page has Offers tab showing property-specific offers with same actions
- Property detail page has Costs tab showing maintenance + compliance breakdown with threshold config
- Landlord page has Costs section showing per-property cost breakdown
- All monetary amounts displayed in GBP (converted from pence)
- /crm/offers route works (placed before /crm catch-all in App.tsx)
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-corrections-cost-ledger/07-04-SUMMARY.md`
</output>
