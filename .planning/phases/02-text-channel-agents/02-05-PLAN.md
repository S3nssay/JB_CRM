---
phase: 02-text-channel-agents
plan: 05
type: execute
wave: 5
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - server/agents/sdk/runner.ts
  - server/agents/services/scheduledMessages.ts
  - server/agentWebhooks.ts
  - tests/agents/memoryInjection.test.ts
  - tests/agents/postActionConfirmation.test.ts
  - tests/agents/followUp.test.ts
autonomous: true
requirements: [CHAN-03, CHAN-04, SALES-04, LETT-04]

must_haves:
  truths:
    - "A second message from the same contact continues in the same conversation thread with context from previous interactions injected into the agent"
    - "After a viewing is booked, the contact receives an immediate confirmation on the same channel"
    - "Viewing reminders are sent at 24 hours before and morning-of the viewing"
    - "WhatsApp/SMS confirmations are sent automatically after agent actions (viewing booked, lead captured)"
    - "Follow-up sequences (Day 1/3/7) execute via pg-boss scheduled jobs"
    - "STOP keyword on any channel immediately halts all AI outreach to that contact"
  artifacts:
    - path: "server/agents/services/scheduledMessages.ts"
      provides: "pg-boss job queue for viewing reminders, follow-up sequences, and checklist chases"
      exports: ["scheduledMessageService"]
    - path: "tests/agents/memoryInjection.test.ts"
      provides: "Tests for conversation history injection into agent context"
    - path: "tests/agents/postActionConfirmation.test.ts"
      provides: "Tests for automatic confirmations after agent actions"
    - path: "tests/agents/followUp.test.ts"
      provides: "Tests for follow-up job scheduling and execution"
  key_links:
    - from: "server/agents/sdk/runner.ts"
      to: "server/agents/channels/conversationStore.ts"
      via: "getConversationHistory() for memory injection"
      pattern: "getConversationHistory"
    - from: "server/agents/services/scheduledMessages.ts"
      to: "server/agents/services/messageSender.ts"
      via: "messageSender.sendPreferred() for scheduled delivery"
      pattern: "messageSender\\.send"
    - from: "server/agentWebhooks.ts"
      to: "server/agents/services/scheduledMessages.ts"
      via: "Post-action hooks trigger confirmations and reminders"
      pattern: "scheduledMessageService"
---

<objective>
Wire cross-channel conversation memory, automatic post-action confirmations, viewing reminders, and follow-up sequences via pg-boss.

Purpose: This plan completes the agent system by ensuring conversations have memory across interactions (CHAN-03), actions trigger automatic confirmations and follow-ups (CHAN-04, SALES-04, LETT-04), and all scheduled messages (viewing reminders, follow-up sequences, checklist chases) are processed by a durable pg-boss job queue.

Output: Full agent pipeline with memory injection, automatic confirmations, scheduled reminders, and follow-up sequences.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-text-channel-agents/02-CONTEXT.md
@.planning/phases/02-text-channel-agents/02-RESEARCH.md
@.planning/phases/02-text-channel-agents/02-01-SUMMARY.md
@.planning/phases/02-text-channel-agents/02-02-SUMMARY.md
@.planning/phases/02-text-channel-agents/02-03-SUMMARY.md
@.planning/phases/02-text-channel-agents/02-04-SUMMARY.md

@server/agents/sdk/runner.ts
@server/agents/sdk/salesAgent.ts
@server/agents/channels/conversationStore.ts
@server/agents/services/messageSender.ts
@server/agentWebhooks.ts

<interfaces>
From server/agents/channels/conversationStore.ts:
```typescript
export class ConversationStore {
  async getConversationHistory(conversationId: number, limit?: number): Promise<Array<{
    role: string;
    content: string;
    senderType: string;
    channel: string;
    createdAt: Date;
  }>>;
  async storeMessage(conversationId: number, message: NormalizedMessage, direction: 'inbound' | 'outbound', agentType?: string): Promise<number>;
}
export const conversationStore: ConversationStore;
```

From server/agents/sdk/runner.ts (created in Plan 01):
```typescript
export async function runAgent(agent: Agent, messageText: string, context: AgentContext): Promise<string>;
```

From server/agents/services/messageSender.ts (created in Plan 01):
```typescript
export class MessageSender {
  send(channel, to, body, options?): Promise<{ success: boolean; messageId?: string }>;
  sendPreferred(phone, body): Promise<{ channel: string; success: boolean }>;
}
export const messageSender: MessageSender;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scheduled message service with pg-boss and post-action confirmation hooks</name>
  <files>
    server/agents/services/scheduledMessages.ts,
    server/agentWebhooks.ts,
    tests/agents/postActionConfirmation.test.ts,
    tests/agents/followUp.test.ts
  </files>
  <behavior>
    - scheduledMessageService.start() initializes pg-boss and registers workers for 'viewing-reminder', 'follow-up-thanks', 'follow-up-similar', 'follow-up-checkin', 'checklist-chase'
    - scheduledMessageService.scheduleViewingReminders(viewingId, contactPhone, channel, viewingDate) queues 2 jobs: 24h before + morning-of
    - scheduledMessageService.scheduleFollowUp(contactPhone, channel, leadType, propertyIds?) queues 3 jobs: Day 1/3/7
    - Each worker processes the job by calling messageSender.sendPreferred()
    - STOP keyword check: before sending any scheduled message, check if contact has opted out
    - Post-action hook: after book_viewing tool completes, immediate confirmation sent + reminders scheduled
    - Post-action hook: after create_lead tool completes, email summary sent
    - Follow-up jobs: Day 1 = "Thank you for your interest in...", Day 3 = "We thought you might also like...", Day 7 = "Just checking in..."
  </behavior>
  <action>
    1. Create `server/agents/services/scheduledMessages.ts`:
       - Import PgBoss from 'pg-boss'
       - Import messageSender from messageSender.ts
       - Import auditLogger from auditLogger.ts

       - Class ScheduledMessageService:
         * private boss: PgBoss
         * `async start()`: Initialize pg-boss with DATABASE_URL, call boss.start(), register workers:
           - `viewing-reminder`: sends reminder message via messageSender.sendPreferred(). Check opt-out before sending
           - `follow-up-thanks`: "Thank you for your interest in [property]. We hope you found it to your liking."
           - `follow-up-similar`: "We thought you might also be interested in these similar properties..." (include propertyIds if available)
           - `follow-up-checkin`: "Just checking in -- are you still looking for properties in [area]? We'd love to help."
           - `checklist-chase`: calls checklistService.chaseItem (import from checklistService)
           - All workers: check opt-out status before sending, log delivery via auditLogger

         * `scheduleViewingReminders(viewingId, contactPhone, channel, viewingDate)`:
           - Calculate 24h before: `new Date(viewingDate.getTime() - 24*60*60*1000)`
           - Calculate morning-of: viewingDate set to 9:00 AM same day
           - Queue 'viewing-reminder' job for each with startAfter, retryLimit: 3, retryDelay: 300
           - Job data: { viewingId, contactPhone, channel, type: '24h' | 'morning' }

         * `scheduleFollowUp(contactPhone, channel, leadType, propertyIds?)`:
           - Queue 'follow-up-thanks' at +1 day
           - Queue 'follow-up-similar' at +3 days
           - Queue 'follow-up-checkin' at +7 days
           - Each job: { contactPhone, channel, leadType, propertyIds }

         * `async checkOptOut(contactPhone: string)`: Query contact_identity for opt-out flag. Return boolean

       - Export singleton scheduledMessageService

    2. Modify `server/agentWebhooks.ts`:
       - Add post-action processing after agent run completes:
         * After runAgent returns, check if the agent made tool calls (inspect run result or use a callback pattern)
         * If book_viewing was called: send immediate confirmation on same channel + schedule viewing reminders + send email summary
         * If create_lead was called: send email summary of the enquiry
         * STOP keyword handling: already exists from Plan 01, but ensure it also sets opt-out flag on the contact_identity record so scheduled messages check it

    3. Write tests:
       - `tests/agents/postActionConfirmation.test.ts`:
         * Mock messageSender and scheduledMessageService
         * Test: after viewing booked, immediate confirmation sent on same channel
         * Test: after viewing booked, 2 reminder jobs scheduled (24h + morning)
         * Test: after viewing booked, email summary sent regardless of original channel
         * Test: after lead captured, email summary sent

       - `tests/agents/followUp.test.ts`:
         * Mock pg-boss
         * Test: scheduleFollowUp queues 3 jobs with correct startAfter dates (Day 1/3/7)
         * Test: scheduleViewingReminders queues 2 jobs (24h before + morning-of)
         * Test: worker skips sending if contact has opted out
         * Test: worker logs delivery via auditLogger
  </action>
  <verify>
    <automated>npx vitest run tests/agents/postActionConfirmation.test.ts tests/agents/followUp.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - pg-boss scheduled message service processes viewing reminders and follow-up sequences
    - Post-action hooks send immediate confirmations and email summaries
    - STOP opt-out checked before every scheduled message
    - Follow-up sequence: Day 1 thanks, Day 3 similar, Day 7 check-in
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire conversation memory injection into agent runner</name>
  <files>
    server/agents/sdk/runner.ts,
    tests/agents/memoryInjection.test.ts
  </files>
  <behavior>
    - runAgent loads last 20 messages from ConversationStore for the conversation
    - Messages are formatted as OpenAI chat format: { role: 'user' | 'assistant', content }
    - History is prepended before the current user message so agents have context
    - If conversation is new (0 messages), no history is prepended
    - Long conversations (30+ messages) are truncated to last 20 to avoid context overflow
    - Agent response references context from earlier messages when relevant
  </behavior>
  <action>
    1. Modify `server/agents/sdk/runner.ts`:
       - In runAgent(), before calling run():
         a. Call conversationStore.getConversationHistory(context.conversationId, 20)
         b. Map history to OpenAI message format:
            - senderType 'contact' or direction 'inbound' -> { role: 'user', content }
            - senderType 'agent' or direction 'outbound' -> { role: 'assistant', content }
         c. Prepend history messages before the current `{ role: 'user', content: messageText }`
         d. Pass the full messages array to run()

    2. Write tests in `tests/agents/memoryInjection.test.ts`:
       - Mock conversationStore.getConversationHistory to return controlled history
       - Mock run() from @openai/agents
       - Test "memory": runAgent with conversationId that has 5 previous messages -> run() receives 6 messages (5 history + 1 new)
       - Test "empty": new conversation with 0 history -> run() receives 1 message
       - Test "truncation": history limited to 20 messages even if more exist
       - Test "format": history messages have correct role mapping (inbound=user, outbound=assistant)
  </action>
  <verify>
    <automated>npx vitest run tests/agents/memoryInjection.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Conversation history from ConversationStore injected into agent run
    - Last 20 messages loaded and formatted as OpenAI chat messages
    - New conversations have no history prepended
    - Tests verify message count, role mapping, and truncation
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/agents/ --reporter=verbose` -- all agent tests pass
- `npx vitest run --reporter=verbose` -- full suite green
- `npm run check` -- TypeScript compilation clean
</verification>

<success_criteria>
- Conversation memory injected from ConversationStore (last 20 messages) into every agent run
- Viewing confirmations sent immediately on same channel after booking
- Viewing reminders scheduled at 24h before and morning-of
- Email summaries sent after significant actions regardless of original channel
- Follow-up sequences (Day 1/3/7) scheduled and executed via pg-boss
- STOP keyword halts all scheduled outreach
- All tests pass, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-text-channel-agents/02-05-SUMMARY.md`
</output>
