/**
 * Agent Runner
 *
 * Executes an OpenAI Agents SDK agent with conversation history,
 * AI identification, and audit logging.
 *
 * Placeholder: full implementation in Task 2.
 */

import type { AgentContext } from './context';

/**
 * Run an agent against a message with full context.
 * Returns the agent's response text.
 */
export async function runAgent(
  _agent: any,
  _messageText: string,
  _context: AgentContext,
): Promise<string> {
  // Stub -- will be fully implemented in Task 2
  throw new Error('runAgent not yet implemented -- see Task 2');
}
